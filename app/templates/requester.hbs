<div class="col-md-6 col-md-offset-3">  
  {{#if responseMessage}}
     <div class="alert alert-success">{{responseMessage}}</div>
   {{/if}}
  <section>
    <h2>List people are request</h2>
    {{#each model as |request|}}
		<p>
		  <span><strong>{{request.email}}</strong></span>
		</p>
    {{/each}}
  </section>
</div>  